# Legacy examples

The JSON and JSONL artifacts in this directory document the earlier guided/v2
demo. They are retained for historical context only.

They are not loaded by the ambient-v3 browser, are not current ObservationV3 or
report examples, and are not validated by the active repository gate.
